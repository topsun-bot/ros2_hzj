<?xml version="1.0" encoding="UTF-8" ?>
<!--
  CONTRACT SEED / 契约种子 — 不是从 topsun_dimos main 提取的配置。

  已在 topsun_dimos @ a5259958db23c8ea6648544ed138eab19726ce93 (main) 搜索：
    fastdds.xml, FASTRTPS_DEFAULT_PROFILES_FILE, ROS_DOMAIN_ID, rmw_fastrtps,
    docker/navigation/
  结论：main 上均不存在。

  R0 冻结（docs/architecture/ros2-dds-r0-interface-freeze.md）链 A：
    RMW = rmw_fastrtps_cpp
    ROS_DOMAIN_ID = 42
    FASTRTPS_DEFAULT_PROFILES_FILE 指向本文件（由 config/env/chain_a.sh 导出）

  本文件只兑现「把契约落成可引用路径 + 与冻结表对齐的域/QoS 种子」，
  不是现网 XML，也不是自定义 RMW。
  域 ID 以环境变量 ROS_DOMAIN_ID=42 为准（须操作员显式 source helper）。
  中文说明：config/fastdds.zh.md

  Humble Fast-DDS (2.6) XMLPARSER: History QoS 必须写在
  <topic><historyQos>，不能写在 <qos><history>。
  2026-09-10 基线日志：Invalid element ... Name: history → loadXMLFile 失败。
  iter1 只改这个 schema 落点；kind/depth 与冻结表相同。

  iter2 (one knob): default participant UDP socket buffers 2 MiB.
  Linux default ~212 KiB is smaller than a 1 MiB sample; ping-pong is
  bidirectional so send and listen are the same knob. Not a second change.

  iter3 (one knob): default-participant RTPS send-buffer pool sized
  for a 1 MiB fragment burst. Humble Fast-DDS 2.6 default
  preallocated_number=0 (thread-count guess) and dynamic=false
  (block when empty). 1 MiB / ~64 KiB ≈ 16 fragments. Not SHM,
  not a second socket-buffer knob. SHM maxMessageSize probe
  regressed same-host 1 MiB and was not kept.

  iter4 (one knob): drop send-buffer prealloc back to the default
  guess (0) and keep dynamic=true. 32 (and a 16 probe) is the booked
  same-host 100/256 KiB p50 tax vs iter2-after; mid-size is ~2–4
  fragments and does not need a 1 MiB-sized slab. 1 MiB may grow
  instead of waiting. Not SHM, not a second socket-buffer knob.
  A 32→16 probe did not recover same-host mid-size and was not kept.
-->
<profiles xmlns="http://www.eprosima.com">
    <participant profile_name="ros2_hzj_chain_a_contract_seed" is_default_profile="true">
        <domainId>42</domainId>
        <rtps>
            <name>ros2_hzj_chain_a_contract_seed</name>
            <sendSocketBufferSize>2097152</sendSocketBufferSize>
            <listenSocketBufferSize>2097152</listenSocketBufferSize>
            <allocation>
                <send_buffers>
                    <preallocated_number>0</preallocated_number>
                    <dynamic>true</dynamic>
                </send_buffers>
            </allocation>
        </rtps>
    </participant>

    <!-- 以下 writer/reader 名称对应冻结表，不是 DimOS publisher 的运行时绑定。 -->
    <data_writer profile_name="foxglove_teleop_to_cmd_vel">
        <topic>
            <historyQos>
                <kind>KEEP_LAST</kind>
                <depth>1</depth>
            </historyQos>
        </topic>
        <qos>
            <reliability>
                <kind>BEST_EFFORT</kind>
            </reliability>
            <durability>
                <kind>VOLATILE</kind>
            </durability>
        </qos>
    </data_writer>
    <data_reader profile_name="foxglove_teleop_to_cmd_vel">
        <topic>
            <historyQos>
                <kind>KEEP_LAST</kind>
                <depth>1</depth>
            </historyQos>
        </topic>
        <qos>
            <reliability>
                <kind>BEST_EFFORT</kind>
            </reliability>
            <durability>
                <kind>VOLATILE</kind>
            </durability>
        </qos>
    </data_reader>

    <data_writer profile_name="goal_pose">
        <topic>
            <historyQos>
                <kind>KEEP_LAST</kind>
                <depth>5</depth>
            </historyQos>
        </topic>
        <qos>
            <reliability>
                <kind>RELIABLE</kind>
            </reliability>
            <durability>
                <kind>VOLATILE</kind>
            </durability>
        </qos>
    </data_writer>
    <data_reader profile_name="goal_pose">
        <topic>
            <historyQos>
                <kind>KEEP_LAST</kind>
                <depth>5</depth>
            </historyQos>
        </topic>
        <qos>
            <reliability>
                <kind>RELIABLE</kind>
            </reliability>
            <durability>
                <kind>VOLATILE</kind>
            </durability>
        </qos>
    </data_reader>

    <data_writer profile_name="way_point">
        <topic>
            <historyQos>
                <kind>KEEP_LAST</kind>
                <depth>5</depth>
            </historyQos>
        </topic>
        <qos>
            <reliability>
                <kind>RELIABLE</kind>
            </reliability>
            <durability>
                <kind>VOLATILE</kind>
            </durability>
        </qos>
    </data_writer>
    <data_reader profile_name="way_point">
        <topic>
            <historyQos>
                <kind>KEEP_LAST</kind>
                <depth>5</depth>
            </historyQos>
        </topic>
        <qos>
            <reliability>
                <kind>RELIABLE</kind>
            </reliability>
            <durability>
                <kind>VOLATILE</kind>
            </durability>
        </qos>
    </data_reader>

    <!-- /joy：冻结表只钉 topic，未规定 QoS，故不写 profile。 -->
    <!-- Go2 ROS bridge 绑定见 config/topics.yaml；QoS 未在冻结表展开。 -->
</profiles>
