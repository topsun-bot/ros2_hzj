# iter4 delta vs iter3 (Chain A only)

Like-to-like remasure is **pending**. This file is a placeholder so in-repo links resolve. Numbers land after `run_large_packet.sh`.

**Do not** put these numbers in a table with Chain B. **Do not** treat same-host localhost as a Feishu / real-robot / cross-host root cause.

这些对照数字 **不是** 飞书现场、实机、或跨机根因证明。

Change under test: default-participant RTPS send-buffer pool `preallocated_number` 32 → 0 (default guess), `dynamic=true` kept. See [`change.md`](change.md).
