# 《3》 评估驱动改进循环日志 — 2026-09-19

> 本仓库是 ROS 2 / DDS **证据/文档型**工作区，不是 LLM 应用。
> 用户原题里"总分与 LLM 平均分 > 90%"的目标在本仓没有对应的 LLM 评分器；
> 本循环把它**诚实映射**为：12 个 gate 脚本 exit-0 率 + 每个 gate 的成功 marker 存在率。
> 不伪造 LLM 分数。

## 0. 开工前

- 已读 `AGENTS.md`（双链契约、Hold 边界、13 个既有 gate）。
- 本机环境：macOS, 无 `/opt/ros/*`, `rclpy` 不可 import, 无 `ros2` CLI。
  这意味着所有"端到端 pub/sub / 时延 p99"类测量在本机 **不可执行**，
  只能跑文件系统/环境字符串类 gate。这一点与
  `docs/architecture/feishu-three-chain-repro.md` 的 `STATUS: blocked` 一致，不伪造通过。

## 1. 评分命令

新引入的统一 runner（行为不变，只是把既有 12 个 gate 串起来）：

```bash
python3 scripts/run_all_gates.py
```

它对每个 gate 同时检查：(a) exit code == 0，(b) 该 gate 文档承诺的
成功 marker 出现在 stdout/stderr 里。两者都满足才算 green。

## 2. 基线（iter 0）

| script | exit | marker |
| --- | --- | --- |
| `scripts/prove_rmw.py` | 0 | ok |
| `scripts/print_bench_gates.py` | 0 | ok |
| `scripts/check_source_map.py` | 0 | ok |
| `scripts/check_risk_matrix.py` | 0 | ok |
| `scripts/check_executor_map.py` | 0 | ok |
| `scripts/check_runtime_provenance.py` | 0 | ok |
| `scripts/check_unitree_cyclone_swap.py` | 0 | ok |
| `scripts/check_three_chain_repro.py` | 0 | ok |
| `scripts/check_sink_layers.py` | 0 | ok |
| `scripts/check_dual_chain_baseline.py` | 0 | ok |
| `scripts/check_dod_evidence.py` | 0 | ok |
| `scripts/check_cega_bridge_hold.py` | 0 | ok |

- **gate exit-0 率：12/12 = 100%**
- **marker 存在率：12/12 = 100%**
- **综合 gate 健康分：100/100**（这是文件系统/字符串闸的诚实分数，
  不是端到端 ROS 2 时延分；后者在本机 `STATUS: blocked`）。

## 3. 迭代日志

### iter 1 — 引入 `scripts/run_all_gates.py`

- **变更**：新增 `scripts/run_all_gates.py`（约 90 行），
  顺序调用 12 个既有 gate，打印 markdown 表 + 汇总。
  不改任何既有 gate、不改 fastdds.xml、不改 SCOREBOARD。
- **为什么**：原来要手敲 12 条命令才能确认全绿；《3》要求"每次有意义修改后重跑评分"，
  一条命令是循环的前提。
- **重跑结果**：12/12 exit 0，12/12 marker ok，runner 自身 exit 0。
- **分数变化**：100 → 100（基线本就全绿；新增 runner 是**效率改进**，不是分数改进）。

### iter 2 — 故意负向验证 runner 真的会红

- **变更**：临时 `mv scripts/check_risk_matrix.py /tmp/risk.bak`，跑 runner，
  确认该行变成 `127 | MISSING`、`non-zero: 1`、`run_all_gates: FAIL`、runner 自身 exit 1，
  然后 `mv` 还原。
- **目的**：证明 runner 不是"永远绿"的橡皮图章。
- **结果**：负向验证符合预期（改前绿、改后红、还原后再绿）。
- **分数变化**：100 → 临时 11/12 ≈ 91.7% → 还原后回到 100。

> iter 2 的临时文件移动已还原，`git status` 应无残留。

## 4. 当前最佳分

- **gate exit-0 率：12/12 = 100%**
- **marker 存在率：12/12 = 100%**
- **runner 自身：exit 0**
- **端到端 ROS 2 时延分：不可测（本机无 Humble runtime），STATUS: blocked**

## 5. 剩余风险与薄弱环节

1. **本机无 ROS runtime**：所有"真 DDS 互通 / p99 时延 / 双链 pub-sub"用例在本机只能
   跑静态检查。要拿到真实分数，需要一台装了 `/opt/ros/humble` 的 Linux 主机
   （参见 `docs/architecture/feishu-three-chain-repro.md`）。
2. **gate 是字符串/文件存在性检查**：它们防止文档撒谎、防止 Hold 被悄悄违反，
   但**不**证明 DDS 真的能跑通。`prove_rmw.py` 自己也明说
   "env and filesystem facts only, not end-to-end delivery"。
3. **cross-host UDP bench 仍 blocked**（见 `docs/artifacts/bench/2026-09-11-cross-host/BLOCKED.txt`）。
4. **Unitree Cyclone wire 兼容性 UNPROVEN**：bundled 0.10.2 vs vendor 11.0.1
   drop-in FAIL（见 `docs/architecture/unitree-sdk2-dds-swap.md`）。
5. **DoD 五项 unmet**：实编、modified `.so`、本机 baseline-vs-change、回滚、产品验收阈
   均未满足（见 `docs/architecture/feishu-dod-evidence.md`）。

## 6. 不做的事（Hold）

- 不改 `config/fastdds.xml`。
- 不改 `docs/artifacts/bench/SCOREBOARD.md` 数字。
- 不启用 Agnocast / zenoh / Cega。
- 不改 `dimos_bridge` 运行时。
- 不伪造 Humble runtime 下的 p99 数字。
