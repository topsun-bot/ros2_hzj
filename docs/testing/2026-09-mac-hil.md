# 《4》 Mac 实测分诊报告 — 2026-09-19

- 主机：macOS（Darwin），zsh
- 工作树：`/Users/zhang/colima-work/ros2_hzj`，branch `main`，up to date with `origin/main`
- ROS runtime：**未安装**（`rclpy` 不可 import，无 `/opt/ros/*`，无 `ros2` CLI）
  → 任何真·pub/sub、p99 时延、跨机 UDP 用例在本机**不可执行**，不伪造。

## 用例选择理由

从本仓库**实际能跑**的路径里选 3 个：
1. 核心用例 1 — 全 gate 套件（`run_all_gates.py`）。
2. 核心用例 2 — `prove_rmw.py` 在伪造环境下的诚实性（DoD 契约）。
3. hero 用例 3 — `config/env/load.py` import 副作用契约（AGENTS.md 明说"import 不写 os.environ"）。

---

## 核心用例 1 — 全 gate 套件

**复现步骤**
```bash
cd /Users/zhang/colima-work/ros2_hzj
python3 scripts/run_all_gates.py; echo "exit=$?"
```

**预期结果**：12 个 gate 全部 exit 0，marker 全 ok，runner 自身 exit 0。

**实际结果**：通过。12/12 exit 0，12/12 marker ok，runner exit 0。
（表格见 `docs/eval/2026-09-loop.md` §2。）

**严重程度**：n/a（通过）。

---

## 核心用例 2 — `prove_rmw.py` 伪造环境诚实性

**复现步骤**
```bash
RMW_IMPLEMENTATION=rmw_fastrtps_cpp \
ROS_DOMAIN_ID=42 \
FASTRTPS_DEFAULT_PROFILES_FILE=/etc/hosts \
python3 scripts/prove_rmw.py
```

**预期结果**：脚本不因为环境变量"看起来对"就声称 RMW 已加载；
必须如实打印 `rclpy: not importable`、`rmw identifier (runtime): (not loaded)`，
并在尾部重申"env and filesystem facts only, not end-to-end delivery"，exit 0。

**实际结果**：通过。脚本打印了
- `RMW_IMPLEMENTATION: rmw_fastrtps_cpp`（env 里的值，作为"请求"）
- `rclpy: not importable (ModuleNotFoundError: No module named 'rclpy')`
- `rmw identifier (runtime): (not loaded)`
- `librmw_*.so: (none on PATH ...)`
- 尾部：`ROS not loaded: rclpy is not importable and no librmw_*.so was found. ... Exit 0.`

**严重程度**：n/a（通过）。DoD 诚实性契约成立。

---

## hero 用例 3 — `config/env/load.py` import 副作用契约

**复现步骤**
```bash
cd /Users/zhang/colima-work/ros2_hzj
python3 - <<'PY'
import os, sys
sys.path.insert(0, "config/env")
before = set(os.environ.keys())
import load
after = set(os.environ.keys())
print("env keys added by import:", sorted(after - before) or "(none)")
PY
```

**预期结果**：`import load` 不向 `os.environ` 写任何键
（AGENTS.md："import of load.py does not write os.environ. Operators must source / apply explicitly."）。

**实际结果**：通过。`env keys added by import: (none)`，
`load.__file__` 指向 `config/env/load.py`。

**严重程度**：n/a（通过）。

---

## Bug 分诊总结

| # | 用例 | 结果 | 严重程度 |
| --- | --- | --- | --- |
| 1 | 全 gate 套件 | PASS | — |
| 2 | prove_rmw 伪造环境诚实性 | PASS | — |
| 3 | load.py import 纯净性 | PASS | — |

**阻塞性问题**：无。

**非阻塞观察 / 限制**
1. 本机无 ROS Humble runtime，无法跑真·pub/sub、p99、跨机 UDP。
   这与 `docs/architecture/feishu-three-chain-repro.md` 的 `STATUS: blocked` 一致，
   不是新发现的 bug。
2. 跨机 UDP bench 目录 `docs/artifacts/bench/2026-09-11-cross-host/` 仍带 `BLOCKED.txt`。
3. 三条链复现 map ≠ reproduce（已知，见 AGENTS.md）。

**后续建议**
- 在一台装了 `/opt/ros/humble` 的 Linux 主机上重跑本用例 1–3，
  并追加两个真·DDS 用例：(a) Chain A `rmw_fastrtps_cpp` / domain 42 pub-sub；
  (b) Chain B Cyclone / domain 0 pub-sub。这两条目前在 macOS 上无法执行。
